import React from 'react';

const EventOptions = () => {
    return (
        <div>
            <h1>Event Options</h1>
            {/* Add your CRUD operations for events here */}
        </div>
    );
};

export default EventOptions;
